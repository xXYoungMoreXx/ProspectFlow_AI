{
  "title": "Hefesto — HITL Queue",
  "uid": "hefesto-hitl",
  "tags": ["hefesto", "hitl"],
  "panels": [
    {
      "title": "HITL Pending by Operator",
      "type": "bargauge",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        { "expr": "hefesto_hitl_pending", "legendFormat": "{{operator_id}}" }
      ]
    },
    {
      "title": "HITL Decisions Rate",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "expr": "rate(hefesto_hitl_approvals_total[5m])",
          "legendFormat": "{{action_type}} {{decision}}"
        }
      ]
    }
  ],
  "schemaVersion": 38,
  "version": 1
}
