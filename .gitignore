# ─── Dependencies ─────────────────────────────
node_modules/
.pnp
.pnp.js

# ─── Build outputs ────────────────────────────
dist/
.next/
out/
build/
*.tsbuildinfo

# ─── Turborepo ────────────────────────────────
.turbo/

# ─── Environment / Secrets ────────────────────
.env
.env.*
!.env.example
/secrets/
*.pem
*.key
*.p12
infisical.json

# ─── IDE ──────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ─── OS ───────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini

# ─── Python ───────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
*.egg-info/
.eggs/
*.egg
uv.lock

# ─── Docker ───────────────────────────────────
infra/data/
infra/volumes/

# ─── Test / Coverage ─────────────────────────
coverage/
.nyc_output/
*.lcov
.pytest_cache/
htmlcov/

# ─── Misc ─────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.cache/
test_output.txt

# ─── Security Reports (GitHub exports — never commit) ─────────────────────────
# Expose unpatched CVEs, secret strings, and CodeQL findings.
# Regenerate from GitHub Security tab when needed; never store in VCS.
secret_scanning_alerts.json
code_scanning_alerts.json
dependabot_alerts.json
open_codeql.json
open_code.txt
open_dep.txt
open_dependabot.json

# ─── ChromaDB local vector store ──────────────────────────────────────────────
# Binary HNSW index + SQLite — machine-specific, may embed ingested user data.
# Repopulate via: python scripts/seed_builder_rag.py
chroma_db/

# ─── Agent / AI tooling config ────────────────────────────────────────────────
# May contain MCP server URLs with internal project refs or local machine paths.
.agent/
.sentinel/
GEMINI.md

# ─── Personal / ephemeral working notes ───────────────────────────────────────
task.md
auth-refactor.md
scratch_*.js
tasks/
# Empty placeholder created by tooling — no real content
ARCHITECTURE.md
# AI session planning docs — ephemeral, regenerated per session
docs/superpowers/plans/

# ─── Git worktrees ────────────────────────────────────────────────────────────
.worktrees/
.clone/
graphify-out/

# ─── Claude Code / AI tooling ─────────────────────────────────────────────────
# settings.local.json is personal (local paths, tokens) — never commit
.claude/settings.local.json
# Claude Code plugin cache (local machine skill downloads)
.claude/plugins/
.claude/skills/
# ad-hoc test/debug scripts that slipped to root
apps/api/test.cjs
# agent output / analysis artifacts that shouldn't be tracked
apps/api/test_output.txt

# Playwright MCP artifacts
.playwright-mcp/
